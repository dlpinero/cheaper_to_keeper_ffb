VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Yahoo app's Client ID (not secret — safe in the frontend bundle). The Client
# Secret is server-side only: set with `supabase secrets set YAHOO_CLIENT_SECRET=...`
# alongside YAHOO_CLIENT_ID, never put it here.
VITE_YAHOO_CLIENT_ID=
